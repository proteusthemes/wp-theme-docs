<ul>
	<li>go to <kbd><kbd><samp>Settings</samp></kbd> → <kbd><samp>Reading</samp></kbd></kbd>, set the the <strong>Front page displays</strong> to <strong>a static page</strong> and select the front page and the blog page in the appropriate dropdowns,</li>
	<li>go to <kbd><kbd><samp>Appearance</samp></kbd> → <kbd><samp>Menus</samp></kbd></kbd>, select the main menu from the dropdown at the top and click on <strong>Select</strong>. Scroll to the bottom and set the <strong>Theme location</strong> to <strong>Main Menu</strong> (if there are more locations you can also assign other menus to those locations),</li>
	<li>go to <kbd><kbd><samp>Appearance</samp></kbd> → <kbd><samp>Widgets</samp></kbd></kbd>, and set <strong>Custom menu</strong> widgets, by selecting the correct menu (in <i>footer</i> and <i>our services</i> sidebars),</li>
	<li>optionally set other things (like logo, colors, ...). These settings can be found in <kbd><kbd><samp>Appearance</samp></kbd> → <kbd><samp>Customize</samp></kbd></kbd>.</li>
</ul>
